# 一级属性规则精简及价格管理原型

目标：按用户确认的文档2（revision 433）扩展现有平台原型，只修改两项属性配置及新价格模块。

- [x] 统一两个菜单、标题、面包屑；删除规则名称及备注/原因输入和必填限制。附加属性值本身的名称与店员确认提示保留。历史数据不删除。
- [x] 新增独立价格管理脚本和样式，沿用现有导航/表格/弹窗；三个页签：马来本地价格、价格策略、计算预览。
- [x] Excel下载和上传预览：型号编码、内存、卖场、币种、本地价格必填；价格来源可选。校验支持型号内存、重复、正数。确认才覆盖当前价格，保存文件、批次、逐行前后值和上传人时间。
- [x] 价格策略：编号版本、品类、全部/指定型号、内存、价格区间、比例及金额调整、优先级、启停和历史。无名称/备注。修改保存新版本；使用独立浏览器存储。
- [x] 预览：本地价格/基准P1×本机P1，然后应用单条策略；无命中不调整；缺价、无效基准、冲突提示原因。策略优先级方向待用户回复，默认沿用数字小优先；金额显示不定义正式取整口径。
- [x] 单元及DOM检查：导入失败不改价、版本与历史、公式、筛选、表单无名称备注、原模块可访问。浏览器实际交互与截图能执行时复核，否则说明限制。
- [x] 仅提交本任务文件，发布并核对线上文件。

技术边界：仅前端原型，不新增PMS/BI真实调用、不修改报告、标单及报价流程。SheetJS 0.20.3本地静态依赖，来源 https://docs.sheetjs.com/docs/getting-started/installation/standalone/ 。

验证：36项单元/静态检查、价格模块DOM、一级属性推断规则DOM及旧配置拆分DOM通过；完整页面浏览器导航与静态页面setContent超时，未完成视觉验收。优先级按数字小优先演示，待用户另行指定时再调整。
